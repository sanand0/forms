<%= _(view.fields).pluck('label').map(function(label) { return '"' + label + '"' } ).join(',') %>
<% var fields = _(view.fields).pluck('name');
   for (var i=0, doc; doc=docs[i]; i++) {
%><%=     _(fields).map(function(field) { return '"' + (doc[field]||'').replace('"', '""') + '"';  }).join(',') %>
<% } %>